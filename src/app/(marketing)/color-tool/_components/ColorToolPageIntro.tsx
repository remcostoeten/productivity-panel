export default function ColorToolIntro() {
  return (
    <div className="flex flex-col">
      <h1 className="text-2xl font-bold pb-2">Color Picker</h1>
      <p className="text-muted-foreground pb-4 border-b border-neutral-700 mb-8">
        The Color Picker feature streamlines color management with an intuitive
        interface for uploading images, picking colors, and generating CSS
        variables. Users can organize colors into folders, manipulate them, and
        get code snippets for easy integration. All data is saved locally for
        easy access.
      </p>
    </div>
  );
}
